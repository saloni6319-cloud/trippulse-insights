# Database Import

Python scripts used to load the generated CSV datasets into the TripPulse Insights MySQL database.

The import workflow uses batch insertion and handles missing values appropriately before loading records into the relational database.
